server:
  port: 9003

spring:
  application:
    name: cloud-alibaba-nacos-provider-payment
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848   # 注册中心

management:
  endpoints:
    web:
      exposure:
        include: "*"
